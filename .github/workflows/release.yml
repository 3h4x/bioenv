name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Git Version
        id: version
        uses: codacy/git-version@2.7.1
        with:
          release-branch: main
          minor-identifier: "feat:"
          major-identifier: "breaking:"
          prefix: "v"

  release:
    needs: version
    if: needs.version.outputs.version != ''
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: |
          swift build -c release
          codesign -s - -f .build/release/bioenv
          cp .build/release/bioenv bioenv-arm64
          chmod +x bioenv-arm64

      - name: Push tag
        env:
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          git tag "$VERSION"
          git push origin "$VERSION"

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          draft: false
          name: ${{ needs.version.outputs.version }}
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ needs.version.outputs.version }}
          artifacts: bioenv-arm64
          artifactContentType: application/octet-stream
          body: |
            ## Install

            ```bash
            curl -L https://github.com/3h4x/bioenv/releases/latest/download/bioenv-arm64 -o ~/bin/bioenv
            chmod +x ~/bin/bioenv
            codesign -s - -f ~/bin/bioenv
            ```
